<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # index.html은 그대로
  RewriteRule ^index\.html$ - [L]

  # 실제 파일이나 폴더가 아닌 경우 index.html로 리디렉션
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>

#test